apiVersion: v1
kind: Secret
metadata:
  name: "mongo-admin-secret"  # corresponds to spec.users.passwordSecretRef.name in the MongoDB CRD
  namespace: mongo
type: Opaque
stringData:
  password: "1PaulMuadib@" # corresponds to spec.users.passwordSecretRef.key in the MongoDB CRD